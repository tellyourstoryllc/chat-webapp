{{! Use a form tag to capture enter key. }}
<form class='form signup-form home-join-group-signup-form' {{action 'attemptSignup' on='submit'}}>
  {{#if errorMessage}}
    <div class='alert alert-error'>
      <span class='close' {{action 'dismissErrorMessage'}}>&times;</span>
      {{errorMessage}}
    </div>
  {{/if}}
  <div class='control-group'>
    {{input type='email' value=email placeholder='Email Address'}}
  </div>
  <div class='control-group'>
    {{input type='text' value=name placeholder='Display Name'}}
  </div>
  <div class='form-actions'>
    <button type='submit' class='app-btn btn-block btn-flat btn-green' {{bind-attr disabled='isSignupDisabled'}}>Enter Room</button>

    {{#if showFacebookChoice}}
      <div class='or-divider'>OR</div>

      <button type='button' {{action 'signUpWithFacebook'}} class='app-btn btn-block btn-flat btn-fb signup-with-facebook-button'>
        <span class='facebook-f-icon'></span>
        <div class='btn-fb-text-container'>
          Connect with Facebook
        </div>
      </button>
    {{/if}}

    <div class='login-hint'>
      Already have an account?  <a href='/login' {{action 'logInWithRoom'}}>Log In</a>
    </div>
  </div>
</form>
