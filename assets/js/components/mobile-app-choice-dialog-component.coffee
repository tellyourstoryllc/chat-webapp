#= require mobile-install-dialog-component

# Same as mobile install dialog, but instead of showing the app store icon,
# shows two buttons to allow the user to install the app or launch it on mobile.
#
# Actions: didDismissMobileInstallDialog
App.MobileAppChoiceDialogComponent = App.MobileInstallDialogComponent.extend()
